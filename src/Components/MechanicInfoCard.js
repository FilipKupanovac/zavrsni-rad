import React from 'react';

//CSS

const MechanicInfoCard = ({name,email}) => {
    return(
        <>
            <p>{name}</p>
            <p>{email}</p>
        </>
    )
}

export default MechanicInfoCard;